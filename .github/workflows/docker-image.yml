name: Docker Image CI

on:
  push:
    branches: [ "staging" ]
  workflow_dispatch: 

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
#     - uses: actions/checkout@v3
#     - name: Build the Docker image
#       run: docker build -t vaibhavrajnathchauhan/sports-academy-backend-staging: .
      - run: echo ${{ github.run_number }}
